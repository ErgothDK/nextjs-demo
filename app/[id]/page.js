import MeetupDetail from "@/components/meetups/MeetupDetail";
import React from "react";

const DetailsMeetupPage = (props) => {
  return <MeetupDetail props={props} />;
};

export default DetailsMeetupPage;
